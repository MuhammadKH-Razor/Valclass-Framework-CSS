if (document.querySelector('div').classList.contains('v-bg-blend-local')) {

    let styles = ` 
            .v-bg-blend-local { 
                background-blend-mode: local;
        }`;

    document.getElementsByTagName('style')[0].appendChild(document.createTextNode(styles));
}

if (document.querySelector('div').classList.contains('v-bg-blend-scroll')) {

    let styles = ` 
            .v-bg-blend-scroll { 
                background-blend-mode: scroll;
        }`;

    document.getElementsByTagName('style')[0].appendChild(document.createTextNode(styles));
}

if (document.querySelector('div').classList.contains('v-bg-blend-local')) {

    let styles = ` 
            .v-bg-blend-local { 
                background-blend-mode: local;
        }`;

    document.getElementsByTagName('style')[0].appendChild(document.createTextNode(styles));
}

if (document.querySelector('div').classList.contains('v-bg-blend-scroll')) {

    let styles = ` 
            .v-bg-blend-scroll { 
                background-blend-mode: scroll;
        }`;

    document.getElementsByTagName('style')[0].appendChild(document.createTextNode(styles));
}

if (document.querySelector('div').classList.contains('v-bg-blend-local')) {

    let styles = ` 
            .v-bg-blend-local { 
                background-blend-mode: local;
        }`;

    document.getElementsByTagName('style')[0].appendChild(document.createTextNode(styles));
}

if (document.querySelector('div').classList.contains('v-bg-blend-scroll')) {

    let styles = ` 
            .v-bg-blend-scroll { 
                background-blend-mode: scroll;
        }`;

    document.getElementsByTagName('style')[0].appendChild(document.createTextNode(styles));
}