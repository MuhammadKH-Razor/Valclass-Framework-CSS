if (document.querySelector('div').classList.contains('v-navbar')) {

    let styles = ` 
            .v-navbar { 
                position: relative;
                width: 100vw;
                height: 40px;
                padding: 5px;
                font-weight: 500;
            }`; 

    document.getElementsByTagName('style')[0].appendChild(document.createTextNode(styles));
}

if (document.querySelector('div').classList.contains('v-jumbotron')) {

    let styles = ` 
            .v-jumbotron { 
                position: relative;
                width: 100vw;
                height: 40px;
                padding: 5px;
                font-weight: 500;
            }`; 

    document.getElementsByTagName('style')[0].appendChild(document.createTextNode(styles));
} 

if (document.querySelector('div').classList.contains('v-sidebar')) {

    let styles = ` 
            .v-sidebar { 
                position: relative;
                width: 100vw;
                height: 40px;
                padding: 5px;
                font-weight: 500;
            }`; 

    document.getElementsByTagName('style')[0].appendChild(document.createTextNode(styles));
}

if (document.querySelector('div').classList.contains('v-section')) {

    let styles = ` 
            .v-section { 
                position: relative;
                width: 100vw;
                height: 300px;
                padding: 5px;
                font-weight: 500;
            }`; 

    document.getElementsByTagName('style')[0].appendChild(document.createTextNode(styles));
}

if (document.querySelector('div').classList.contains('v-footer')) {

    let styles = ` 
            .v-footer { 
                position: relative;
                width: 100vw;
                height: 40px;
                padding: 5px;
                font-weight: 500;
            }`; 

    document.getElementsByTagName('style')[0].appendChild(document.createTextNode(styles));
}

if (document.querySelector('div').classList.contains('v-card')) {

    let styles = ` 
            .v-card { 
                position: relative;
                width: 100vw;
                height: 40px;
                padding: 5px;
                font-weight: 500;
            }`; 

    document.getElementsByTagName('style')[0].appendChild(document.createTextNode(styles));
}

if (document.querySelector('div').classList.contains('v-btn')) {

    let styles = ` 
            .v-btn { 
                position: relative;
                width: 100vw;
                height: 40px;
                padding: 5px;
                font-weight: 500;
            }`; 

    document.getElementsByTagName('style')[0].appendChild(document.createTextNode(styles));
}